all:
  children:
    dev:
      hosts:
        portal:
          ansible_host: api-dev.slateci.io
#    prod:
#      hosts:
#        portal:
#          ansible_host: api.slateci.io
  vars:
    ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q <slate-user>@bastion.slateci.net -i <priv-key-path>"'
    ansible_ssh_private_key_file: '<priv-key-path>'
    ansible_user: '<slate-user>'
