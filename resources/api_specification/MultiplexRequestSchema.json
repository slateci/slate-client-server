{
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema",
  "items": {
    "type": "object",
    "properties": {
      "method": {
        "type": {
          "enum": ["DELETE", "GET", "HEAD", "POST", "PUT", "CONNECT", "OPTIONS", "TRACE", "PATCH", "PURGE"]
        }
      },
      "body": {
        "type": "string"
      }
    }
    "required": ["method"]
  }
}